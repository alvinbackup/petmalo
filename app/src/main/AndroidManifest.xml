<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pet_malo">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PETMalo"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".My_cart"
            android:exported="false" />
        <activity android:name=".reserved_list" />
        <activity android:name=".product_details" />
        <activity android:name=".product_row" />
        <activity android:name=".product" />
        <activity android:name=".review_reserveprod" />
        <activity android:name=".prod_category" />
        <activity android:name=".petcare_detailed" />
        <activity android:name=".petcare" />
        <activity android:name=".storepage" />
        <activity android:name=".mapstore" />
        <activity android:name=".petlist" />
        <activity android:name=".petcard" />
        <activity android:name=".flashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".AboutUs" />
        <activity android:name=".Appointment" />
        <activity android:name=".user_info" />
        <activity android:name=".profile" />
        <activity android:name=".register" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>